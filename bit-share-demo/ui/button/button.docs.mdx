---
labels: ['react', 'jsx', 'button']
description: 'A Button component.'
---

import { Button } from './button';

A simple Button component with some text. The button has two different style: `primary` and `secondary`.

You can add an `onClick` listener to this component to run code when this button is clicked.

Code Snippet:
```js
<Button>click me</Button>
```


Live Playground:
```js live
<Button>click me</Button>
```
